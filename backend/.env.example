# Example connection string to a MongoDB Atlas cluster named "Talent".
# Replace <username> and <password> with your actual credentials.
MONGODB_URI=mongodb+srv://<username>:<password>@talent.mongodb.net/talent?retryWrites=true&w=majority

# Secret used to sign JWT tokens
JWT_SECRET=
